# Crimson Channels

Discord-inspired Discourse theme for senin.me. The theme stays aligned with native Discourse theme APIs, light/dark color variables, responsive layouts, accessibility, and Official Discourse Theme CI.

This repository also includes the Minimum Token Context v3 workflow under `AGENTS.md`, `docs/ai/`, `.agents/`, `.claude/`, and `.codex/` so future AI-assisted work can load only the smallest relevant context.
